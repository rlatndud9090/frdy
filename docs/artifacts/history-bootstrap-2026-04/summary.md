# History Bootstrap Summary

이 문서는 artifact 체계 도입 이전의 `frdy` 구현 히스토리를 사후 복원한 요약입니다.

## 사용한 근거

- 현재 코드 구조 (`src/`, `data/`, `main.lua`)
- 기존 루트 설계 문서의 현재형 요약
- git log의 커밋 메시지와 시기별 변화 흐름

## 복원된 큰 흐름

### 1. 코어 뼈대와 맵 생성

- 2026-02-07에 게임 싱글턴, Scene 관리, EventBus, 절차적 맵 생성이 먼저 자리잡았습니다.
- 초기에 맵은 Node-Edge 그래프와 절차적 생성기 중심으로 설계되었습니다.

### 2. Scene 통합과 런타임 단순화

- 2026-02-09 전후로 다중 Scene 분리 구조가 `GameScene + phase state machine`으로 합쳐졌습니다.
- 이 결정으로 맵 이동, 전투, 이벤트, 정산, 경로 선택을 하나의 런타임 루프에서 관리하게 되었습니다.

### 3. 개입 시스템의 구조 전환

- 2026-02-12에 Deck/Card 구조가 `SpellBook/Spell` 구조로 바뀌었습니다.
- 이때부터 “드로우 기반 덱빌딩”보다는 “항상 사용 가능한 개입 주문 세트”가 핵심 모델이 되었습니다.

### 4. 예측 타임라인 전투의 정착

- 2026-02-13을 중심으로 Planning/Execution 2페이즈, `PredictionEngine`, `TimelineManager`, 조작/삽입형 스펠, `TimelineUI`가 갖춰졌습니다.
- 이후 2026-02-25 이후 대상 선택, 속도 개입, 정신 조종, 전체 대상 규칙이 더해지며 현재형 전투 루프가 완성됐습니다.

### 5. 보상/성장 및 결정론화

- 2026-03-02에 정산 기반 보상 시스템과 각성/전설 아이템 루프가 추가되었습니다.
- 2026-03-04에 `RunContext` 기반 결정론 RNG가 들어오면서 맵, 적, 이벤트, 보상 등 주요 흐름이 시드 기반으로 재현 가능해졌습니다.

## 한계

- 이 artifact는 작업 중 실시간으로 쌓인 원본 artifact가 아닙니다.
- 회의록, 세부 ADR, PR 리뷰 논점은 일부 손실되었고, 커밋 메시지와 현재 코드에서 복원 가능한 수준만 반영했습니다.

## 후속 운영 원칙

- 2026-04-14 이후의 작업부터는 branch/work unit 단위 artifact를 먼저 만들고 누적합니다.
- 이후 wiki는 이 artifact를 근거로만 갱신합니다.
